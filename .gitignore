# Build outputs
build/
build-tests/
bin/
obj/
dist/
release/

# Pixi environment
.pixi/

# Vendored game DLLs (referenced at build time, never committed)
libs/

# Visual Studio / IDEs
.vs/
.vscode/
.idea/
*.user
*.suo

# Agent tooling (machine-generated, not tracked)
.claude/
.lab/
CLAUDE.md
AGENTS.md
.mcp.json

# Per-game install state
.headtracking-state.json

# NexusMods page copy source (not shipped)
NEXUS_MODS.md

# Reverse-engineering scratch. Ignored from the outset, not after the fact:
# once a decompiler export, trace, memory dump or screenshot is pushed, deleting
# the branch does not unpublish it - the host keeps serving the blobs by SHA.
tools/
scratch/
re/
ghidra/
*.gpr
*.rep/
dumps/
traces/
screenshots/
*.dmp
